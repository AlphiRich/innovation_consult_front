import { PagePlaceholder } from '@/app/PagePlaceholder';

export function FieldDiaryPage() {
  return (
    <PagePlaceholder
      title="Field Diary"
      route="/diary"
      status="stub"
      note="Street-level completion % is derived, never stored (§6.3)."
    />
  );
}
