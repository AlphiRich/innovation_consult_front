import { PagePlaceholder } from '@/app/PagePlaceholder';

export function ScheduledReportsPage() {
  return (
    <PagePlaceholder
      title="Automated Reporting"
      route="/analytics/scheduled-reports"
      status="stub"
      note="Scheduling, distribution lists, notification toggles, watermark control. Retained deliberately — not scope creep (master index §3.2)."
    />
  );
}
