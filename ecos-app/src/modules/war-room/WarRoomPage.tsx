import { PagePlaceholder } from '@/app/PagePlaceholder';

export function WarRoomPage() {
  return (
    <PagePlaceholder
      title="War Room"
      route="/war-room"
      status="stub"
      note="Must read pre-aggregated counter documents only — never a live listener on a full collection. See §7.4 and §8.1."
    />
  );
}
