{
  "name": "ecos-app-functions",
  "private": true,
  "version": "1.0.0-dev",
  "description": "Election Campaign OS — Cloud Functions 2nd gen, africa-south1. IC-ECOS-BUILD-2026-V2 §0 rule 1.",
  "type": "commonjs",
  "engines": { "node": "20" },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "lint": "eslint --ext .js,.ts .",
    "serve": "npm run build && firebase emulators:start --only functions",
    "test": "vitest run",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.5.0",
    "firebase-functions": "^5.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  }
}
